---
phase: 05-frontend-ux
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_fe.py
autonomous: true
requirements:
  - FE-1
  - FE-2
  - FE-3
  - FE-4
  - FE-5
  - FE-6

must_haves:
  truths:
    - "tests/test_fe.py exists with 6 test stubs, one per FE requirement"
    - "Running pytest tests/test_fe.py -x -q exits with code 1 (stubs fail with pytest.fail, not ERROR)"
    - "All stubs use pytest.fail() so failures are FAILED not ERROR"
  artifacts:
    - path: "tests/test_fe.py"
      provides: "Test scaffold for FE-1 through FE-6"
      contains: "test_ranking_sort, test_map_data_score, test_stale_flag, test_indicator_json, test_vessel_permalink, test_csv_export"
  key_links:
    - from: "tests/test_fe.py"
      to: "tests/conftest.py"
      via: "app_client fixture (function scope)"
      pattern: "def test_.*app_client"
---

<objective>
Create the Wave 0 test scaffold: tests/test_fe.py with 6 failing stubs covering FE-1 through FE-6.

Purpose: Nyquist compliance — all verification commands in plans 05-01 through 05-03 reference test functions that must exist before implementation begins.
Output: tests/test_fe.py with 6 pytest.fail() stubs that produce FAILED (exit code 1), not ERROR.
</objective>

<execution_context>
@C:/Users/ardal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ardal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_fe.py with 6 failing stubs</name>
  <files>tests/test_fe.py</files>
  <action>
Create tests/test_fe.py with exactly 6 test stubs. Each stub uses pytest.fail() (produces FAILED exit code 1, not ERROR). All stubs use the existing app_client fixture from conftest.py (function scope). Do NOT use sqlite_db — app_client already handles DB setup.

```python
"""
tests/test_fe.py — Phase 5 Frontend UX test stubs (Wave 0 / RED phase).

All tests use pytest.fail() so the suite exits with code 1 (FAILED)
not code 2 (ERROR). This is the Wave 0 RED state per 05-VALIDATION.md.
"""
import pytest


def test_ranking_sort(app_client):
    """FE-1: /api/vessels/ranking returns vessels sorted by composite_score desc."""
    pytest.fail("stub — implement in plan 05-02")


def test_map_data_score(app_client):
    """FE-2: Map data dict includes composite_score field (not None by default)."""
    pytest.fail("stub — implement in plan 05-02")


def test_stale_flag(app_client):
    """FE-3: is_stale flag propagates correctly in ranking API response."""
    pytest.fail("stub — implement in plan 05-02")


def test_indicator_json(app_client):
    """FE-4: indicator_json present in /api/vessels/ranking response."""
    pytest.fail("stub — implement in plan 05-03")


def test_vessel_permalink(app_client):
    """FE-5: GET /vessel/<imo> returns 200 (or 404 for unknown) with HTML."""
    pytest.fail("stub — implement in plan 05-01")


def test_csv_export(app_client):
    """FE-6: GET /export/vessels.csv returns text/csv with correct column headers."""
    pytest.fail("stub — implement in plan 05-03")
```

Do not add any imports beyond pytest. The app_client fixture is defined in tests/conftest.py (already exists).
  </action>
  <verify>
    <automated>cd "C:/Users/ardal/OneDrive/Desktop/Claude Projects/maritime-osint" && python -m pytest tests/test_fe.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest tests/test_fe.py exits with code 1; output shows "6 failed" with FAILED markers (no ERROR markers); all 6 test names visible in output.</done>
</task>

</tasks>

<verification>
Run: python -m pytest tests/test_fe.py -q
Expected: 6 failed, 0 errors, exit code 1.
Every test shows "FAILED" not "ERROR" — confirms pytest.fail() used correctly.
</verification>

<success_criteria>
tests/test_fe.py exists with 6 stubs covering FE-1 through FE-6. pytest tests/test_fe.py exits 1 with 6 FAILED (not ERROR). Plans 05-01, 05-02, 05-03 can now reference these test functions in their verify blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-ux/05-00-SUMMARY.md`
</output>
