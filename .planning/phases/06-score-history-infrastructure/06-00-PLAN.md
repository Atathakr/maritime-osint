---
plan: "00"
phase: 6
wave: 0
goal: Create the test scaffold for Phase 6 — four failing pytest stubs that define the acceptance contract for HIST-01 and HIST-02.
requires: []
estimated_minutes: 5
---

# Plan 6-00 — Test Stubs (RED)

## Goal

Create `tests/test_hist.py` with four `pytest.fail("stub")` stubs that will be made to pass by Plan 6-01.

## Context

Plans 6-01 through 6-01 (Wave 1) each have a `<verify>` step that runs specific stubs from this file. The stubs must exist — and fail — before any implementation begins. This is the Wave 0 contract: red tests only, no implementation.

The test module follows the same pattern as `tests/test_scores.py`:
- `os.environ["DATABASE_URL"] = ""` at module level (forces SQLite before any db import)
- `_setup_db(monkeypatch)` helper via `monkeypatch.setenv + db._init_backend() + db.init_db()`
- `db._sqlite_path()` for raw sqlite3 verification queries
- `app_client` fixture for Flask route tests (from `conftest.py`)
- Auth via `with app_client.session_transaction() as sess: sess["authenticated"] = True`

IMO range for Phase 6 tests: `IMO8000001` onward (avoids collisions with Phases 2-5).

## Tasks

### Task 6-00-01 — Create `tests/test_hist.py` with four failing stubs

**What**: Write the stub file exactly as shown below. Each stub calls `pytest.fail("stub")` immediately — no assertions, no imports beyond what pytest needs at module level.

**File(s)**: `tests/test_hist.py`

**Why**: Wave 1 tasks each verify against named stubs in this file. The stubs must exist before Wave 1 begins so that `pytest tests/test_hist.py::test_name -x -q` resolves cleanly (FAILED, not ERROR).

Write `tests/test_hist.py` with this exact content:

```python
# tests/test_hist.py
"""
Phase 6: Score History Infrastructure — acceptance tests.
Requirements: HIST-01, HIST-02

Stubs written in Wave 0 (Plan 6-00). Made to pass in Wave 1 (Plan 6-01).

IMO range: IMO8000001+ (no collision with Phases 2-5).
"""
import os
import pytest

os.environ["DATABASE_URL"] = ""  # Force SQLite; must precede any db import


# ── HIST-01: history row is written when score changes ────────────────────────

def test_history_row_written(monkeypatch):
    """HIST-01: A history row is written when composite_score or is_sanctioned changes."""
    pytest.fail("stub")


def test_no_spurious_row(monkeypatch):
    """HIST-01: No history row is written when the score is identical to the last snapshot."""
    pytest.fail("stub")


# ── HIST-02: /api/vessels/<imo>/history endpoint ──────────────────────────────

def test_history_endpoint(app_client):
    """HIST-02: GET /api/vessels/<imo>/history returns up to 30 rows, newest first."""
    pytest.fail("stub")


def test_history_endpoint_404(app_client):
    """HIST-02: GET /api/vessels/<imo>/history returns 404 for unrecognized IMO."""
    pytest.fail("stub")
```

Do not add any imports beyond `os` and `pytest`. Do not add helper functions. The stubs must remain exactly as shown so Wave 1 can replace `pytest.fail("stub")` with real assertions.

<verify>pytest tests/test_hist.py -q 2>&1 | tail -5</verify>

Expected output: `4 failed` (all stubs fail). If any stub shows as an ERROR (not FAILED), fix the import that is causing the collection error before proceeding.
