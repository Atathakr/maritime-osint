---
phase: 08-vessel-profile-enrichments
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_profile_enrichments.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "tests/test_profile_enrichments.py exists with 4 stubs that all fail (pytest.fail)"
    - "All 4 stubs are collected by pytest without import errors"
    - "Stub names match the automated commands in 08-VALIDATION.md exactly"
  artifacts:
    - path: "tests/test_profile_enrichments.py"
      provides: "4 failing test stubs for PROF-01 and PROF-02"
      contains: "pytest.fail"
  key_links:
    - from: "tests/test_profile_enrichments.py"
      to: "tests/conftest.py"
      via: "app_client fixture import"
      pattern: "app_client"
---

<objective>
Create tests/test_profile_enrichments.py with 4 failing stubs — RED phase.

Purpose: Nyquist rule compliance. Wave 1 implementation will turn each stub GREEN. Stubs define the acceptance boundary before any code is written.
Output: tests/test_profile_enrichments.py with 4 pytest.fail() stubs.
</objective>

<execution_context>
@C:/Users/ardal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ardal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-vessel-profile-enrichments/08-CONTEXT.md
@.planning/phases/08-vessel-profile-enrichments/08-VALIDATION.md
</context>

<interfaces>
<!-- From tests/test_alerts.py — stub pattern to replicate exactly -->
<!-- Module header: imports os, pytest. Sets DATABASE_URL="" before any db import. -->
<!-- Stub body: pytest.fail("stub") — no other logic -->
<!-- IMO range for Phase 8: IMO0200001+ (new range, no collision with prior phases) -->

From tests/conftest.py (available fixture):
  app_client — Flask test client with session support via session_transaction()

From .planning/phases/08-vessel-profile-enrichments/08-VALIDATION.md — exact test names:
  test_profile_has_history_card    (PROF-01: smoke — HTML contains card skeleton + CDN tag)
  test_history_single_snapshot     (PROF-01: unit  — API returns valid JSON for 1 snapshot)
  test_change_log_diff             (PROF-02: unit  — diff logic returns correct delta/fired/cleared)
  test_change_log_identical_snapshots (PROF-02: unit — identical snapshots returns "no changes" signal)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_profile_enrichments.py with 4 failing stubs</name>
  <files>tests/test_profile_enrichments.py</files>
  <action>
Create tests/test_profile_enrichments.py. Follow the exact stub pattern from tests/test_alerts.py:
- Module docstring referencing Phase 8, PROF-01, PROF-02, IMO range IMO0200001+
- Import os and pytest at module level only (no db import at module level)
- Four test functions, each body is: pytest.fail("stub")

Exact function signatures (names must match 08-VALIDATION.md):

```python
# tests/test_profile_enrichments.py
"""
Phase 8: Vessel Profile Enrichments — acceptance tests.
Requirements: PROF-01, PROF-02

Stubs written in Wave 0 (Plan 08-00). Made to pass in Wave 1 (Plan 08-01).

IMO range: IMO0200001+ (no collision with Phases 2-7).
"""
import os
import pytest


def test_profile_has_history_card(app_client):
    """PROF-01: /vessel/<imo> HTML contains #score-history-card and Chart.js CDN script tag."""
    pytest.fail("stub")


def test_history_single_snapshot(app_client):
    """PROF-01: /api/vessels/<imo>/history with exactly 1 row returns valid JSON (not an error)."""
    pytest.fail("stub")


def test_change_log_diff(app_client):
    """PROF-02: Given 2 snapshots with score delta and indicator changes, change log returns expected delta/fired/cleared."""
    pytest.fail("stub")


def test_change_log_identical_snapshots(app_client):
    """PROF-02: Identical consecutive snapshots produce the 'no changes since last run' signal."""
    pytest.fail("stub")
```

No database setup helpers, no fixture calls beyond app_client — stubs have no body logic.
  </action>
  <verify>
    <automated>cd "C:/Users/ardal/OneDrive/Desktop/Claude Projects/maritime-osint" && rtk pytest tests/test_profile_enrichments.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>pytest collects 4 tests, all 4 fail with FAILED (not ERROR), no import errors. Output contains "4 failed".</done>
</task>

</tasks>

<verification>
All 4 stubs collected and failing:
  rtk pytest tests/test_profile_enrichments.py -v -q
  Expected: 4 FAILED, 0 errors
</verification>

<success_criteria>
- tests/test_profile_enrichments.py exists with exactly 4 test functions
- All 4 fail on pytest.fail("stub") — not on ImportError or fixture errors
- Test names match 08-VALIDATION.md exactly (no typos)
- app_client fixture resolves without error (conftest.py is already wired)
</success_criteria>

<output>
After completion, create .planning/phases/08-vessel-profile-enrichments/08-00-SUMMARY.md
</output>
